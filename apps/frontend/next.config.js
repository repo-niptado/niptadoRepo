// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // ✅ Make sure nothing experimental or unsupported like `trackDynamicImports` is here
};

module.exports = nextConfig;
